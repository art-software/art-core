{
  "name": "art-ssr-render",
  "version": "0.0.1",
  "description": "art ssr render service",
  "license": "MIT",
  "scripts": {
    "clean": "sudo rimraf -g dist",
    "build": "npm run clean && cpx 'src/**/*.{json,jsx,html,less,css,png,jpg,jpeg,gif,eot,svg,ttf,woff}' ./dist && tsc -p ./tsconfig.json -w"
  },
  "dependencies": {
  },
  "devDependencies": {
    "@types/node": "^10.5.6",
    "cpx": "^1.5.0",
    "tslint": "^5.11.0",
    "typescript": "^3.0.1"
  }
}
