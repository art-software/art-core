{
  "name": "art-lib-common",
  "version": "0.0.1",
  "description": "art common framework agnostic lib",
  "main": "index.js",
  "author": "Bowen Zhong",
  "license": "MIT",
  "scripts": {
    "clean": "sudo rimraf dist",
    "build": "npm run clean && cpx 'src/**/*.{json,jsx,html,less,css,png,jpg,jpeg,gif,eot,svg,ttf,woff}' ./dist && tsc -p ./tsconfig.json -w"
  },
  "dependencies": {
    "art-lib-utils": "^0.0.5",
    "axios": "^0.18.0",
    "cpx": "^1.5.0",
    "typescript": "^3.0.1"
  }
}
