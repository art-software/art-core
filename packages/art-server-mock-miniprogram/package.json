{
  "name": "art-server-mock-miniprogram",
  "version": "0.0.2",
  "description": "art framework mock server for miniprogram",
  "main": "index.js",
  "author": "Bowen Zhong",
  "license": "MIT",
  "scripts": {
    "clean": "sudo rimraf -g dist",
    "build": "npm run clean && cpx 'src/**/*.{json,jsx,html,less,css,png,jpg,jpeg,gif,eot,svg,ttf,woff}' ./dist && tsc -p ./tsconfig.json -w"
  },
  "dependencies": {
    "art-dev-utils": "^0.0.13",
    "compression": "^1.7.3",
    "cpx": "^1.5.0",
    "dotenv": "^6.0.0",
    "express": "^4.16.3",
    "reflect-metadata": "^0.1.12",
    "routing-controllers": "^0.7.7",
    "typedi": "^0.8.0"
  },
  "devDependencies": {
    "@types/express": "^4.16.0"
  }
}
