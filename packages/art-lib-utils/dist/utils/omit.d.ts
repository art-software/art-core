/**
 * Remove values from an object (or an array of objects) based on key, value or
 * an evaluator function.
 * @param rule
 * @param target
 */
export default function (rule: string[] | string, target?: any): any;
